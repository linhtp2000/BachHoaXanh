@model IEnumerable<BachHoaXanh.Data.Models.Category>
@*<ul class="nav navbar-nav nav_1">
        @foreach (var danhmuc in Model)
        {
            <li><a href=""> @danhmuc.Name</a></li>
        }

    </ul>*@
@foreach (var danhmuc in Model)
{
    <li class="dropdown mega-dropdown active">

        <a href="/Classify/Classify?CategoryID=@danhmuc.Id"> @*class="dropdown-toggle" data-toggle="dropdown">*@ @danhmuc.Name  @*<span class="caret"></span>*@</a>

        <!--<div class="dropdown-menu mega-dropdown-menu w3ls_vegetables_menu">
            <div class="w3ls_vegetables">
                <ul>
                    <span class="caret"></span>-->
                    @*<a href="/Classify/Classify?CategoryID=@danhmuc.Id">@ViewBag.classify</a>*@
                    @*@Html.Action("Classify", "Classify") ;*@

                <!--</ul>
            </div>
        </div>-->
    </li>
}