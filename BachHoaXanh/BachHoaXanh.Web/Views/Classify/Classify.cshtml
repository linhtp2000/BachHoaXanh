@model IEnumerable<BachHoaXanh.Data.Models.Classify>
@*@foreach (var nhomhang in Model)
    {
        <li><a href="/Products/Index?id=@nhomhang.Id"> @nhomhang.Name</a></li>
    }*@


@foreach (var nhomhang in Model)
{
    <class ="dropdown-toggle" data - toggle="dropdown">
        <div class="dropdown-menu mega-dropdown-menu w3ls_vegetables_menu">
            <div class="w3ls_vegetables">
                <ul>
                    <span class="caret"></span>
                    <li><a href="/Products/Index?id=@nhomhang.Id"> @nhomhang.Name</a></li>
                </ul>
            </div>
        </div>
    </class>
}